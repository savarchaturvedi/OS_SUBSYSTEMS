The code is compatible ONLY with gcc version 11.2 only make sure to run the following code before executing the Makefile or make command 

module load gcc-11.2

Then execute the make command to make directory savar_out for storing the output and then running and checking with actual outputs.
